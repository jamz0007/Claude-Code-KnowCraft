---
argument-hint: <source> [--type=markdown|bookmarks|notion|obsidian] [--target=DIR] [--recursive] [--auto-tag] [--dry-run]
description: 导入外部知识到知识库
allowed-tools: Read, Write, Bash, Glob
---

# /kb-import - 导入外部知识

## 功能
从各种外部来源导入知识到个人知识库，支持 Markdown 文件、浏览器书签、Notion 和 Obsidian。

## 使用方式

### 导入 Markdown 文件
```
/kb-import /path/to/notes --type=markdown

导入指定目录下的所有 Markdown 文件。
```

### 递归导入
```
/kb-import /path/to/notes --type=markdown --recursive

递归导入子目录中的文件。
```

### 导入浏览器书签
```
/kb-import bookmarks.html --type=bookmarks

从浏览器导出的 HTML 书签文件导入。
```

### 导入 Notion
```
/kb-import notion-export.zip --type=notion

从 Notion 导出的 ZIP 文件导入。
```

### 导入 Obsidian
```
/kb-import /path/to/vault --type=obsidian

从 Obsidian vault 导入。
```

### 自动标签化
```
/kb-import /path/to/notes --type=markdown --auto-tag

自动分析内容并生成标签。
```

### 试运行
```
/kb-import /path/to/notes --dry-run

预览导入结果，不实际导入。
```

## 参数说明

- `source`: 源文件或目录路径 (必需)
- `--type`: 导入类型
  - `markdown`: Markdown 文件 (默认)
  - `bookmarks`: 浏览器书签
  - `notion`: Notion 导出
  - `obsidian`: Obsidian vault
- `--target`: 目标目录 (默认: knowledge/items/)
- `--recursive`: 递归处理子目录
- `--auto-tag`: 自动生成标签
- `--dry-run`: 试运行，不实际导入
- `--preserve-structure`: 保留原始目录结构
- `--convert-links`: 转换内部链接为知识链接

## 导入类型详解

### 1. Markdown 导入

#### 基本用法
```
/kb-import ~/Documents/notes --type=markdown
```

#### 保留目录结构
```
/kb-import ~/Documents/notes --type=markdown --preserve-structure

原始结构:
  notes/
    ├── react/
    │   ├── hooks.md
    │   └── components.md
    └── vue/
        └── basics.md

导入后:
  knowledge/items/2025/01-January/
    ├── react-hooks.md
    ├── react-components.md
    └── vue-basics.md

或 (使用 --preserve-structure):
  knowledge/items/2025/01-January/
    ├── react/
    │   ├── hooks.md
    │   └── components.md
    └── vue/
        └── basics.md
```

#### 自动识别元数据
```yaml
# 源文件可能包含的元数据
---
title: "React Hooks 指南"
tags: [react, hooks, frontend]
date: 2025-01-04
category: learning
---

这些元数据会被保留并转换为知识库格式。
```

#### 无元数据文件处理
```
# 源文件没有元数据时:
文件名: hooks.md
第一级标题: # React Hooks 完全指南

自动提取:
  title: "React Hooks 完全指南"
  type: "default"
  tags: 从内容中提取
```

### 2. 浏览器书签导入

#### Chrome/Edge 书签
```
/kb-import bookmarks.html --type=bookmarks

支持的格式:
  Chrome 导出的 bookmarks.html
  Edge 导出的 bookmarks.html
```

#### 处理逻辑
```
1. 解析 HTML 文件
2. 提取书签链接
3. 提取标题和文件夹
4. 生成知识条目
5. 自动创建标签(基于域名和文件夹)

示例:
  书签: https://react.dev/learn
  标题: React – Learn React

  生成条目:
    title: "React – Learn React"
    type: "bookmark"
    tags: ["react", "frontend", "documentation"]
    url: "https://react.dev/learn"
    source: "chrome-bookmarks"
```

#### 自动分类
```
基于书签文件夹自动分类:
  - Development/ → code 分类
  - Learning/ → learning 分类
  - Work/ → projects 分类
  - 其他 → personal 分类
```

### 3. Notion 导入

#### 从 ZIP 导入
```
/kb-import notion-export.zip --type=notion

Notion 导出步骤:
1. 在 Notion 中选择页面
2. Export → Markdown & CSV
3. 下载 ZIP 文件
4. 使用本命令导入
```

#### 处理 Notion 特性
```
Notion 特性 → 知识库映射:

- 页面 → 知识条目
- 子页面 → 关联的知识条目
- 数据库 → 多个相关条目
- 标签 → 保留并转换
- 日期 → 元数据
- 关联属性 → 链接
- 代码块 → 保留格式
- 嵌入内容 → 转换为链接
```

#### Notion 数据库处理
```
Notion 数据库会被展开为多个条目:
数据库: React 学习笔记
  ├─ useState 笔记 → 条目1
  ├─ useEffect 笔记 → 条目2
  └─ useContext 笔记 → 条目3

关联:
  所有条目自动建立 related 关联
  添加"notion-database"标签
```

### 4. Obsidian 导入

#### 从 Vault 导入
```
/kb-import ~/Documents/ObsidianVault --type=obsidian

支持的 Obsidian 特性:
- Markdown 文件
- Wiki-links [[link]]
- Tags #tag
- Frontmatter
- 文件夹结构
```

#### 转换 Wiki 链接
```
Obsidian 链接 → 知识库链接:

[[React Hooks]]
  → 如果"React Hooks"条目存在，转换为知识链接
  → 如果不存在，创建待办列表

[[Hooks/useState]]
  → 转换为嵌套结构或扁平化
```

#### 转换标签
```
Obsidian 标签 → 知识库标签:

#react/hooks → 标签: ["react", "hooks"]
#frontend/react → 标签: ["frontend", "react"]
```

#### 保留元数据
```
Obsidian frontmatter 会被保留:
---
tags: [react, hooks]
category: learning
status: in-progress
---

转换后保留所有字段。
```

## 导入流程

### 1. 预处理
```
- 扫描源文件
- 统计文件数量
- 识别文件类型
- 检查格式兼容性
```

### 2. 内容提取
```
- 读取文件内容
- 解析元数据
- 提取文本内容
- 识别结构
```

### 3. 内容转换
```
- 转换为知识库格式
- 生成时间戳 ID
- 添加 frontmatter
- 创建关联
```

### 4. 索引更新
```
- 添加到 index.json
- 更新 tags.json
- 更新 relationships.json
- 更新统计信息
```

### 5. 验证确认
```
- 显示导入统计
- 列出导入的条目
- 报告问题和警告
- 生成导入报告
```

## 导入报告

### 成功导入
```
✅ 导入完成!

来源: ~/Documents/notes
类型: markdown
文件数: 15 个

导入统计:
  成功: 15 个
  跳过: 0 个
  失败: 0 个

生成的条目:
  [2026-01-04-120001] React Hooks 指南
  [2026-01-04-120002] Vue.js 基础
  [2026-01-04-120003] JavaScript 异步编程
  ... (共 15 个)

分配的标签:
  react (3 个)
  vue (2 个)
  javascript (5 个)
  frontend (8 个)
  tutorial (6 个)

创建的关联:
  自动发现并创建了 5 个关联

导入时间: 2.3 秒
```

### 带警告的导入
```
⚠️  导入完成，但有警告

来源: ~/Documents/notes
文件数: 20 个

导入统计:
  成功: 18 个
  跳过: 2 个
  失败: 0 个

警告:
  ⚠️  重复文件: hooks.md (已存在，已跳过)
  ⚠️  缺少标题: untitled.md (使用文件名作为标题)

建议:
  - 检查跳过的文件
  - 补充缺失的标题
  - 检查自动生成的标签
```

### 试运行报告
```
🔍 试运行结果 (未实际导入)

来源: ~/Documents/notes
文件数: 15 个

将要导入:
  [预计] hooks.md → React Hooks 指南
  [预计] components.md → React 组件
  ... (共 15 个)

预计标签:
  react (3 个)
  frontend (8 个)

预计关联:
  发现 3 个潜在关联

确认导入? (yes/no):
```

## 高级功能

### 批量处理
```
/kb-import ~/notes1 ~/notes2 ~/notes3 --type=markdown

批量导入多个目录。
```

### 过滤导入
```
/kb-import ~/notes --type=markdown --include="*.md" --exclude="draft/*"

仅导入匹配的文件。
```

### 自定义映射
```
/kb-import ~/notes --map-tags="js:javascript,ts:typescript"

映射标签名称。
```

### 增量导入
```
/kb-import ~/notes --incremental

仅导入新增或修改的文件。
```

## 注意事项

### 1. 文件大小限制
- 单个文件 < 1MB
- 总大小建议 < 100MB

### 2. 编码问题
- 建议使用 UTF-8 编码
- 自动检测并转换编码

### 3. 图片处理
- 本地图片: 复制到知识库
- 远程图片: 保留链接
- 建议手动检查图片链接

### 4. 链接转换
- 内部链接: 尝试转换为知识链接
- 外部链接: 保留原样
- 断开链接: 标记为待修复

### 5. 重复检测
- 基于标题检测重复
- 基于内容相似度检测
- 询问用户如何处理

## 错误处理

### 文件读取失败
```
❌ 错误: 无法读取文件 xxx.md
原因: 权限不足
建议: 检查文件权限
```

### 格式错误
```
❌ 错误: 文件格式不正确
文件: broken.md
问题: Markdown 格式错误
建议: 检查文件格式
```

### 导入失败
```
❌ 导入失败

失败统计:
  无法读取: 2 个
  格式错误: 1 个
  超出大小: 1 个

详细错误列表:
  - file1.md: 权限不足
  - file2.md: 编码不支持
  ...
```

## 导入后操作

### 1. 检查导入结果
```
/kb-list --by=recent --limit=20
```

### 2. 验证内容
```
/kb-search --tag=imported
```

### 3. 创建关联
```
/kb-link id1 id2 --type=related
```

### 4. 优化标签
```
@knowledge-manager "优化导入内容的标签"
```

## 最佳实践

### 1. 先试运行
```
/kb-import ~/notes --dry-run
检查无误后再实际导入
```

### 2. 分批导入
```
按主题或类型分批导入，便于管理
```

### 3. 备份原文件
```
导入前备份原始文件
```

### 4. 使用自动标签
```
/kb-import ~/notes --auto-tag
让 AI 帮助生成标签
```

### 5. 导入后整理
```
导入后立即整理:
  - 检查标题
  - 调整分类
  - 创建关联
  - 补充标签
```

## 与其他命令的配合

### 导入后搜索
```
/kb-import ~/notes
/kb-search --tag=imported
```

### 导入后建立关联
```
/kb-import ~/notes
@knowledge-manager "为新导入的内容创建关联"
```

### 导入后导出
```
/kb-import ~/notes --type=markdown
/kb-export --type=markdown --target=backup
```

## 扩展功能(未来)

### 1. 更多导入源
```
- Evernote 导入
- OneNote 导入
- Roam Research 导入
- Apple Notes 导入
```

### 2. 智能导入
```
AI 自动分类和标签化
```

### 3. 自动同步
```
定期同步外部来源
```

### 4. 增量更新
```
仅更新变化的文件
```
